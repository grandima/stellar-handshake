[package]
name = "utils"
version = "0.1.0"
edition = "2021"

[lib]
name = "utils"
path = "src/lib.rs"

[dependencies]
dryoc = { workspace = true }
ring = { workspace = true }
data-encoding = {workspace = true}